{
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the custom image to be created."
    },
    "instanceId": {
      "type": "string",
      "description": "The id of the instance from which the custom image will be created."
    }
  },
  "required": [
    "name",
    "instanceId"
  ],
  "type": "object"
}